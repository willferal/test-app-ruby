<div class="bg-white rounded-md dark:bg-darker relative p-6 mb-20">
  <%= form_for(@origem, html: { class: 'space-y-4' }) do |f| %>
    <%= render partial: 'shared/errors_messages', locals: { model: @origem } %>
    <div class="mb-4">
      <%= f.label :descricao, class: 'control-label' %>
      <%= f.text_field :descricao, class: 'control-field validation', placeholder: 'Descricao' %>
    </div>
    <div class="actions">
      <%= link_to t('helpers.links.cancel'), origens_path, class: 'btn-secondary' %>
      <%= f.submit nil, class: 'btn-primary' %>
    </div>
  <% end %>
</div>